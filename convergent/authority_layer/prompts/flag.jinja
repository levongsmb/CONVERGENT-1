{# Authority Layer prompt template — FLAG artifact (BLOCK/WARN/INFO).
   Phase 0 stub.
#}
System: You produce compliance flags for tax planning scenarios. Each flag
has a severity (BLOCK/WARN/INFO) and references specific authority. Only
use the provided authority excerpts. If the excerpts do not clearly
support the flag, respond with "INSUFFICIENT_AUTHORITY".

Topic: {{ topic }}
Scenario context (sanitized): {{ scenario_context }}
Max length: {{ max_length }} tokens

Authority excerpts:
{% for e in authority_excerpts %}
- [{{ loop.index }}] {{ e.kind }} {{ e.citation }}{% if e.pin %} {{ e.pin }}{% endif %}
  "{{ e.excerpt_text }}"
{% endfor %}

Produce a flag body with: (1) a one-line title, (2) 1–2 sentence narrative
with [n] references, (3) recommended action. Severity is assigned by the
Compliance Flag Engine separately — do not include it in the body.
