# Convergent Build Prompt — Consolidated v3

This file holds the canonical build specification supplied by the user. All
phase work references section numbers (§1 through §19) from this document.

If/when the user revises the prompt, a new decision is opened in
`docs/decisions/` recording the delta and the impact on open phases.

> Note: the full prompt text is provided to Claude Code in the initial
> session and is preserved in version control here. Large updates to the
> prompt should create `PROMPT_V4.md` alongside, with a migration decision.

---

The v3 prompt text is supplied in-session and governs the build. See the
user's initial message for the canonical copy. Short references throughout
the codebase use the form `§<section>.<subsection>` (e.g., `§12A.8`,
`§6B.4.1`).
