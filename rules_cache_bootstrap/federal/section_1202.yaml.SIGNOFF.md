# SIGNOFF — `federal/section_1202.yaml`

**Reviewer:** (awaiting CPA initials)
**Review date:** 2026-04-18
**Target YAML:** `section_1202.yaml`

## Signed-off (per Decision 0001 answer 2026-04-18, §1202 full parameter set)

All parameters below were explicitly confirmed by the user with full
values and pin cites.

### OBBBA regime (QSBS issued AFTER 2025-07-04)

- `applicable_date` = 2025-07-04 — **signed_off**
- `tier_3yr_exclusion_pct` = 50% — **signed_off**
- `tier_4yr_exclusion_pct` = 75% — **signed_off**
- `tier_5yr_exclusion_pct` = 100% — **signed_off**
- `per_issuer_cap_dollar_component` = $15,000,000 — **signed_off**
- `per_issuer_cap_dollar_indexing_start_year` = 2027 — **signed_off**
- `per_issuer_cap_basis_multiplier` = 10× — **signed_off**
- `gross_assets_ceiling_at_issuance` = $75,000,000 — **signed_off**
- `gross_assets_ceiling_indexing_start_year` = 2027 — **signed_off**

### Rate treatment

- `non_excluded_gain_rate` = 28% under §1(h)(4) — **signed_off**
- `niit_treatment_of_excluded_gain` = `EXCLUDED_FROM_NIT_BASE` — **signed_off**
- `excluded_gain_amt_adjustment` = `NO_ADJUSTMENT` — **signed_off**

### Pre-OBBBA regime (QSBS issued on or before 2025-07-04)

- Pre-OBBBA $10M / 10× cap, $50M gross assets, 5-year hold — **signed_off**

## Authority of record

- IRC §1202(a), (b)(4), (d)(1) as amended by OBBBA §70431
- P.L. 119-21, 139 Stat. 72 (July 4, 2025)

## Overall sign-off

```
Reviewed and approved: (awaiting CPA countersignature) — <initials>
```
